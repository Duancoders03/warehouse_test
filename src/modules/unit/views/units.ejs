<div class="space-y-6">
  
  <!-- Header -->
  <div class="flex items-center justify-between border-b border-slate-200 pb-4">
    <div>
      <h1 class="text-2xl font-bold tracking-tight text-slate-900">Danh Mục Đơn Vị Tính</h1>
      <p class="text-sm text-slate-500 mt-1">Quản lý chuẩn hóa các đơn vị đo lường vật tư & hàng hóa (Cột D Mẫu 01-VT)</p>
    </div>
  </div>

  <!-- Filter & Search Bar -->
  <div class="bg-white p-4 rounded-2xl border border-slate-200/80 shadow-sm">
    <div class="max-w-md">
      <label class="block text-xs font-semibold text-slate-600 mb-1">Tìm Kiếm Đơn Vị Tính</label>
      <div class="relative">
        <input type="text" id="unitSearchKeyword" placeholder="Nhập mã ĐVT hoặc tên đơn vị..." class="w-full text-sm pl-9 pr-3 py-2 border border-slate-300 rounded-xl focus:ring-2 focus:ring-sky-500 focus:border-sky-500 outline-none">
        <i class="fa-solid fa-magnifying-glass absolute left-3 top-2.5 text-slate-400 text-sm"></i>
      </div>
    </div>
  </div>

  <!-- Units Table -->
  <div class="bg-white rounded-2xl border border-slate-200/80 shadow-sm overflow-hidden">
    <div class="overflow-x-auto">
      <table class="w-full text-left border-collapse text-sm" id="unitsTable">
        <thead>
          <tr class="bg-slate-50 text-slate-500 text-xs uppercase font-semibold border-b border-slate-200">
            <th class="py-3.5 px-6 w-20">STT</th>
            <th class="py-3.5 px-6">Mã ĐVT (Code)</th>
            <th class="py-3.5 px-6">Tên Đơn Vị Tính</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-slate-100">
          <% if (units && units.length > 0) { %>
            <% units.forEach((u, idx) => { %>
              <tr class="unit-row hover:bg-slate-50/80 transition-colors"
                  data-search="<%= (u.code + ' ' + u.name).toLowerCase() %>">
                <td class="py-4 px-6 text-slate-500 font-mono text-xs"><%= idx + 1 %></td>
                <td class="py-4 px-6 font-mono font-bold text-sky-700">
                  <span class="px-2.5 py-1 rounded-lg bg-sky-50 text-sky-700 border border-sky-200 font-bold text-xs">
                    <%= u.code %>
                  </span>
                </td>
                <td class="py-4 px-6 font-bold text-slate-900"><%= u.name %></td>
              </tr>
            <% }) %>
          <% } else { %>
            <tr>
              <td colspan="3" class="py-12 text-center text-slate-400">
                Chưa có dữ liệu đơn vị tính.
              </td>
            </tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>

</div>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const keywordInput = document.getElementById('unitSearchKeyword');
    const rows = document.querySelectorAll('.unit-row');

    keywordInput?.addEventListener('input', () => {
      const keyword = keywordInput.value.toLowerCase().trim();

      rows.forEach(row => {
        const searchStr = row.dataset.search || '';
        if (!keyword || searchStr.includes(keyword)) {
          row.style.display = '';
        } else {
          row.style.display = 'none';
        }
      });
    });
  });
</script>
